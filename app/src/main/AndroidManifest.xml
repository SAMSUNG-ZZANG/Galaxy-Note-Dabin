<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.sopt.androidassignment">

    <application
        android:name=".SOPTApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AndroidAssignment">
        <activity
            android:name=".SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="org.sopt.auth.signin.SignInActivity"
            android:exported="false" />
        <activity
            android:name="org.sopt.auth.signup.SignUpActivity"
            android:exported="false" />
        <activity
            android:name="org.sopt.home.HomeActivity"
            android:exported="false" />
    </application>

</manifest>